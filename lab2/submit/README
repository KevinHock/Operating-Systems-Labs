A shell with command parsing, changing the directory, debugging messages, variable support, redirection on arbitrary handles (no pipes though), history tracking and a signature wolfie command among other things.
